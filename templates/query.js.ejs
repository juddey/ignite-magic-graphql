import React from "react"
import gql from "graphql-tag"
import { Query } from "react-apollo"

const GET_QUERY = gql`
  {
    // Your query here
  }
`

const <%= props.name %> = ({ onChangeFunc }) => (
  <Query query={GET_QUERY}>
    {({ loading, error, data }) => {
      if (loading) return "Loading..."
      if (error) return `Error! ${error.message}`

      return (
        /* Your JSX here */
      )
    }}
  </Query>
)